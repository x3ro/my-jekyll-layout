$(document).ready(function() {

    /*
    $('pre').each(function() {
        if(!this.className.match(/\bno-prettyprint\b/)) {
            this.className += ' prettyprint';
        }
    });
    */

    prettyPrint();

});
